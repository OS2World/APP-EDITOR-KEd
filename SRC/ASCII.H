
SHORT ascii_chart (SHORT code, char *str);

void show_ascii_chart (void);

/* Include ascii box in do_basics() function.
   Invoke ascii box by pressing Alt-A during edit.
   Uncomment the following #define to exclude this feature and save code. */
